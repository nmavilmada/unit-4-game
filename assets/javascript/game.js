// set styles to change button color when pressed
var Styles = {
    color : "yellow" ,
    backgroundColor : "green" ,
    fontWeight: "bold",
}

// counters for the number of clicks per each crystal
var counter1 = 0;
var counter2 = 0;
var counter3 = 0;
var counter4 = 0;

// random number generator for pc between 120 and 30
var compRand = Math.floor((Math.random() * 90) + 30);

// random number generator for crytals 1-4 between numners 1 and 12
var crystalnumber1 = Math.floor((Math.random() * 12) + 1);
var crystalnumber2 = Math.floor((Math.random() * 12) + 1);
var crystalnumber3 = Math.floor((Math.random() * 12) + 1);
var crystalnumber4 = Math.floor((Math.random() * 12) + 1);

// stores the multplied random generated numbers for crystals 1 through 4
var crystalRand1;
var crystalRand2;
var crystalRand3;
var crystalRand4;
var crystaltotal;
   

// start game button will initiate the game and generate a random number that should be matched
// by numbers generated by the four crystals.

// set Initialize Game to false 



var initializeGame = false;


$("#startGame").click(function () {
    
    initializeGame = true;
    $(this).css(Styles); 
    $(this).css('backgroundColor','red');
    $(".compNumber").html(compRand);
    
        if (initializeGame = true) {
    
        $("#crystal1").click(function (){
        $(this).css(Styles);
        counter1++
        crystalRand1 = crystalnumber1 * counter1
        $(".crystal1").html(crystalRand1);   
        })

        $("#crystal2").click(function (){
        $(this).css(Styles);
        counter2++
        crystalRand2 = crystalnumber2 * counter2
        $(".crystal2").html(crystalRand2);   
        // $(".totalcrystals").html(crystaltotal);   
        })
      
        $("#crystal3").click(function (){
        $(this).css(Styles);
        counter3++
        crystalRand3 = crystalnumber3 * counter3
        $(".crystal3").html(crystalRand3); 
        // $(".totalcrystals").html(crystaltotal);  
        })

        $("#crystal4").click(function (){
        $(this).css(Styles);
        counter4++
        crystalRand4 = crystalnumber4 * counter4
        $(".crystal4").html(crystalRand4);    
        // $(".totalcrystals").html(crystaltotal);  
        })
        
      
        // if (compRand === crystalRand1 || compRand === crystalRand2 ||compRand === crystalRand3 ||compRand === crystalRand4 || compRand === crystaltotal){
        // alert (" you won")  ;
        // }
        // if (crystalRand1 > compRand || crystalRand2 > compRand || crystalRand3 > compRand || crystalRand4 > compRand || crystaltotal > compRand ){
        // alert (" you lost")
        // }

    }
})